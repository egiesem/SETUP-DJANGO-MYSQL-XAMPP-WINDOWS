1. Install XAMPP
2. Install python3, pip
3. Install wheel : pip install wheel 
   and download https://www.lfd.uci.edu/~gohlke/pythonlibs/ 
   and : >pip install mysqlclient-1.4.6-cp38-cp38-win32.whl
4. Install pip install mysql-connector-python dan pip install mysql-connector or pip install mysqlclient


example config:

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'latihan',
        'USER': 'root',
        'PASSWORD': 'root',
        'HOST': '127.0.0.1',
        'PORT': '3306',
    }
}
